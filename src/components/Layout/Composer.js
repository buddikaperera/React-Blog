import React, { Component } from "react";

export default class Composer extends Component {
  render() {
    return (
      <div>
        <textarea />
        <button>Send</button>
      </div>
    );
  }
}
